## Machine Team：20 个 AI Agent 组成的多角色协作编排系统

我基于 Claude Code 构建了一套完整的多 Agent 协作系统，包含 1 个总调度、6 个专业组、20 个专职 Agent，覆盖缺陷修复、功能开发、技术债务清理、技术调研、风险缓解、知识管理六大领域。

### 核心设计

**角色分离与权力制衡**：每个 Agent 有明确的职责边界、决策权（否决权/判断权/建议权三级划分）和制衡方。不是简单的"多个 AI 各干各的"，而是设计了质疑-回应、独立审核、双线并行、最终验收、实证推翻五种制衡模式，让 Agent 之间形成真实的张力。

**双盲审计机制**：缺陷修复时，安全审计和架构审计并行执行，互不可见对方报告。所有跨审计信息必须脱敏中转，强制独立判断，禁止附和。这解决了 AI 最致命的问题——单点判断偏差。实测中，双盲审计能发现单个 Agent 审查遗漏的边界条件和架构退化。

**流程刚性与信号驱动**：每个组有严格的 SOP，任务必须按步骤执行，不可跳步。组间通过信号自动触发协作（如发现技术债务自动通知债务清理组），不依赖人工判断。任务完成后自动检查是否触发知识沉淀，形成"做事-验证-沉淀"的闭环。

### 技术实现

**系统级强制执行**：通过 Claude Code 的 PreToolUse/PostToolUse Hook 机制，将工作流协议从"建议"升级为"物理约束"。实现了 7 个 Hook：路由门禁（拦截未分类的任务）、代码门禁（阻止未路由的代码修改）、审计隔离（物理阻断双盲审计的信息泄露）、跨组信号检测、SOP 执行强制、日志维护强制、状态清理。Hook 通过 stdin JSON 协议与 Claude Code 交互，exit code 控制放行/阻断。

**文件契约体系**：所有 Agent 产出物落盘为结构化文件，带 YAML 元数据（创建时间、作者、任务 ID、会话 ID）。会话数据按任务分区，知识条目带时效标注和来源引用。文件是角色间的显性契约，不是信息容器。

### 实际效果

在修复实际 Bug 的测试中，系统表现：程序员完成修复后，双盲审计发现了单个 Agent 审查遗漏的安全边界条件；QA 验证否决了第一轮修复（回归测试失败），程序员根据 QA 的失败证据重新修复后通过。整个过程无需人工干预，但每一步都有独立视角审查。技术债务信号自动触发后续清理流程，知识管理组从任务产出中提取了可复用的根因分析模式。

这套系统的价值不在于用了什么模型，而在于定义了"多个 AI 如何协作、制衡、验证"的工程方法论。模型越强，产出越复杂，人工审查越难，这套架构越有价值。